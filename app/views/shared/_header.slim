header.side-content.fixed-top
  .logo-wrap
    .logo
      span.helper
      = link_to image_tag('logo.png'), root_path, class: 'logo-link'
    #menu-toggle.menu-toggle-wrap
      .menu-toggle.collapsed
        .icon-bar
        .icon-bar.icon-bar--half
        .icon-bar
  .menu-wrap.collapsed
    .media.user-profile-menu
      .media-image
      .media-body.milli = presented_current_user.name
    ul.list-striped
      .borrowed-books
        - presented_current_user.currently_borrowed_books.each do | book|
          li.media
            .media-image
            .media-body
              .book-item
                .book-title.milli.truncate = book.title
              .book-action = book.action
    #close-menu 
      a href='#'
        = t('menu.close')
